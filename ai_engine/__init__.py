"""AI layer: nutrition maths, rule-based diet engine and optional LLM providers.

This package has no web-framework dependencies so it can run inside an API server,
a serverless function or a background worker.
"""
