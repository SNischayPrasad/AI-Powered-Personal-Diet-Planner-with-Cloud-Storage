# Development & test dependencies — install with:  pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
moto[s3]>=5.0        # in-memory AWS S3 used to test the S3 storage adapter offline
ruff>=0.6            # linter + import sorter (includes security checks)
